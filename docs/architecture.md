## Architecture Overview

```
Client (React)
      │
      ▼
REST API (Spring Boot)
      │
      ▼
Authentication Layer (Spring Security + JWT)
      │
      ▼
Service Layer
      │
      ▼
Repository Layer (JPA)
      │
      ▼
Database (MySQL)
```
